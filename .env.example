# PostgreSQL will be connected in the next stage.
DATABASE_URL=postgresql://user:password@host:5432/laminat_sale

# Public origin used for redirects, webhooks and absolute links.
APP_URL=http://localhost:3000

# Never commit real values. Store production secrets in the hosting provider.
ADMIN_SESSION_SECRET=replace-with-a-long-random-secret
